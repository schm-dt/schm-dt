{
  "name": "schm-dt",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna exec -- yarn start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "lerna": "^3.22.1"
  }
}
